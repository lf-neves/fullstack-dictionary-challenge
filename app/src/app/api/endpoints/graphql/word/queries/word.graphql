query Word($wordId: ID!) {
  word(wordId: $wordId) {
    wordId
    word
    isFavorite
    phonetics {
      text
      audio
    }
    meanings {
      partOfSpeech
      definitions {
        definition
        example
      }
      synonyms
      antonyms
    }
  }
}
