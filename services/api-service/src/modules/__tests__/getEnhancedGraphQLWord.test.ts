// test("does not return an inactive word", async () => {});

// test("throws an error if the requested word does not exist", async () => {});

// test("throws an error if the dictionary API request fails", async () => {});
