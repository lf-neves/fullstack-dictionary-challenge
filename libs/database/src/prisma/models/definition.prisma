model Definition {
  definitionId         String     @id @default(uuid())
  definition String
  example    String?
  meaningId  String
  meaning    Meaning @relation(fields: [meaningId], references: [meaningId])
}
